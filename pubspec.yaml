name: circular_doubly_linked_list
description: A high-performance Circular Doubly Linked List with O(1) operations and optimized bidirectional traversal.
version: 1.0.2
repository: https://github.com/da578/circular_doubly_linked_list

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
